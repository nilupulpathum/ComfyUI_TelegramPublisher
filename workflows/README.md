# Example Workflows

Store minimal, dependency-light example workflow JSON files here.

Required examples:
1. `basic_send_image.json`
2. `send_album.json`
3. `metadata_caption.json`

Never include real Telegram tokens, private chat IDs, or private prompts in examples.
